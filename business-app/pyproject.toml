[project]
name = "vbiz-skills-platform"
version = "0.1.0"
description = "Nền tảng quản trị doanh nghiệp VN tích hợp Agent Skills (FastAPI + OpenAI)"
requires-python = ">=3.11"
license = { text = "MIT" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
